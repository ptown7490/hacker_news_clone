<% content_for(:title, 'Links | HackerClone') %>
<%= link_to 'New link', new_link_path, :class => 'btn btn-default' %>
<h1>List of links</h1>

<ul>
  <% @links.each do |link| %>
    <li><a href='<%= link.content_url %>'><%= link.title %></a></li>
  <% end %>
</ul>
